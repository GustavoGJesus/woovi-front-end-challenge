// styles
import styled from "@emotion/styled";
import { MediaQuerys } from "../../styles/global";

export const PaymentMethodContainer = styled.main`
    padding: 36px 15px 36px 15px;
`;

export const PaymentMethodContent = styled.header`
    display: flex;
    flex-direction: column;

    align-items: center;
`;

export const CardsContainer = styled.div`
    display: flex;
    flex-direction: column;
    margin-top: 32px;
`;